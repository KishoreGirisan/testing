pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building..'
            }
        }
        stage('Test') {
            steps {
               var test = readFile 'sample.html',
                echo test
            }
        }
        
    }
}
