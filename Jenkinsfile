pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building..'
            }
        }
        stage('Test') {
            steps {
               def test = readFile 'sample.html',
                echo test
            }
        }
        
    }
}
